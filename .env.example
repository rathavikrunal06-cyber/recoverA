# This project currently has no required environment variables.
# Keep any future payment-provider credentials out of Git and configure them
# in the deployment platform's environment-variable settings.
